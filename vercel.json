{
  "builds": [
    {
      "src": "package.json",
      "use": "@now/node",
      "config": { "maxLambdaSize": "50mb" }
    }
  ],
  "version": 2,
  "routes": [{ "src": "/.*", "dest": "api/index.js" }],
  "build": {
    "scripts": {
      "build": "prisma generate && next build"
    }
  }
}
